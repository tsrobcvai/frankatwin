[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "frankatwin"
version = "0.2.0"
description = "1 kHz Cartesian impedance controller for Franka FR3/Panda with a sim-to-real system-ID pipeline (IsaacLab)."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
authors = [
    { name = "Tao Sun", email = "tao.sun@mail.mcgill.ca" },
    { name = "Patrick Yin" },
]
keywords = ["franka", "libfranka", "impedance-control", "system-identification", "sim-to-real", "isaac-lab"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Robotics",
]
dependencies = [
    "numpy>=1.23",
    "pyyaml>=6.0",
    "pyzmq>=25",
]

[project.optional-dependencies]
test = [
    "pytest>=7",
]
# For scripts/compare_sim_real.py and friends (PC-side analysis).
analysis = [
    "pandas>=1.5",
    "matplotlib>=3.5",
]

[project.urls]
Homepage = "https://github.com/tsrobcvai/frankatwin"
Documentation = "https://github.com/tsrobcvai/frankatwin/tree/main/docs"
Issues = "https://github.com/tsrobcvai/frankatwin/issues"

[tool.setuptools]
package-dir = { "" = "python" }

[tool.setuptools.packages.find]
where = ["python"]
include = ["frankatwin*"]
